# Local OS cruft
.DS_Store

# Inputs & outputs live on the gh-pages branch, checked out locally as the
# published/ worktree (see CLAUDE.md). They are never committed to main.
/published

# Thread JSON dropped at the repo root — move it into published/threads/.
/*.json
